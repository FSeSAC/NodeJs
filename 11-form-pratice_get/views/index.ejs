<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Get으로 정보 받기</title>
</head>
<body>
    <h1><%= title %></h1>

    <form action="/getForm" method="get">
        <label for="name" name="name">이름</label>
        <input type="text" name="name" id="name">

        <fieldset>
            <legend>성별</legend>
            <input type="radio" id="male" name="gender" value="male">
            <label for="male">남자</label>
            <input type="radio" id="male" name="gender" value="femail">
            <label for="femail">여자</label>
        </fieldset>
        <fieldset>
            <legend>생년월일</legend>
            <select name="year" id="year">
                <% for(let i = 2024; i >= 1900; i--) { %>
                    <option value="<%= i %>"><%= i %></option>
                <% } %>
            </select>
            <label for="year" name="year">년</label>
            <select name="month" id="month">
                <% for(let i = 1; i <= 12; i++) { %>
                    <option value="<%= i %>"><%= i %></option>
                <% } %>
            </select>
            <label for="month" name="month">월</label>
            <select name="day" id="day">
                <% for(let i = 1; i <= 31; i++) { %>
                    <option value="<%= i %>"><%= i %></option>
                <% } %>
            </select>
            <label for="day" name="day">일</label>
        </fieldset>
        <fieldset>
            <legend>관심사</legend>
            <input type="checkbox" id="travel" name="interest" value="travel">
            <label for="travel" name="travel">여행</label>
            <input type="checkbox" id="fashion" name="interest" value="fashion">
            <label for="fashion" name="fashion">패션</label>
            <input type="checkbox" id="food" name="interest" value="food">
            <label for="food" name="food">음식</label>
        </fieldset>
        <p></p>
        <button type="submit">회원가입</button>
    </form>
</body>
</html>